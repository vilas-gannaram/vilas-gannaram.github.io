---
title: 'Contains Duplicate'
summary: 'LeetCode 217 - Contains Duplicate. Brute force and optimized solutions.'
tags: ['arrays-hashing', 'hashmap', 'easy', 'python3']
publishedAt: '2026-09-17'
---

**Link:** [leetcode.com/problems/contains-duplicate](https://leetcode.com/problems/contains-duplicate/)

## Problem

Given an integer array `nums`, return `true` if any value appears **at least twice** in the array, and return false if every element is distinct.

## Brute force

Compare every pair(`i` and `j`) of elements and check if they match

```python
def containsDuplicate(self, nums: list[int]) -> bool:
    l = len(nums)
    for i in range(l):
        for j in range(i + 1, l):
            if nums[i] == nums[j]:
                return True
    return False
```

- **Complexity:** O(n^2) time, O(1) space

## Optimized approach

Instead of re-scanning, keep track of what we've already seen — if we find a duplicate, return early.

```python
def containsDuplicate(self, nums: list[int]) -> bool:
    seen = {}

    for num in nums:
        if num in seen:
            return True
        seen[num] = num

    return False
```

- **Complexity:** O(n) time, O(n) space
